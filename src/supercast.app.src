{application, supercast,
    [
        {description, "Supercast, InterFace Session/authentication"},
        {vsn, "0.3.0"},
        {modules, []},
        {registered, [
                supercast_sup,
                supercast_mpd
            ]},

        {included_applications, [ranch, cowboy]},
        {start_phases, [{listen, []}]},
        {applications,
            [kernel, stdlib, xmerl]
        },
        {start_phases, []},
        {mod, {supercast_app, []}}
    ]
}.
